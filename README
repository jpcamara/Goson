Goson uses the Gosu Open Type System to create first-class Gosu types from a JSON schema. Just place a sample
JSON schema in your project, and Goson will creates types from it. The package is currently derived from
the name of the file. 
For instance,
twitter.status.Response.json will create a package of json.twitter.status. "Response" will be used as
the name of the top level object in the JSON file. The type of each property works off of the rules of
Simple JSON Schema [more to follow]